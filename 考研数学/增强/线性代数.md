1. 若B和A可交换，则AB=BA

1. r(A,B)代表把A，B拼起来之后的秩

1. 如果一个实对称矩阵不是满秩的，他也一定可以被相似对角化，因为实对称矩阵的k重特征值一定对应k个线性不相关的特征向量，而他一定有0特征值；所以要注意，一个矩阵能不能被相似对角化只看一件事：k重特征值是否对应k个线性不相关的特征向量

2. 证明向量组线性无关时，如果实在没有思路，可以将最基础的形式写出来，即 $k_1 a_1 + k_2 a_2 + \cdots = 0$ ，证明上式只有在k全为0时才成立