1. 有随机变量X，Y，X服从分布E(1),Y=min(X,1),求Y的分布函数和概率密度(分段)
2. 计算出的所有的概率密度/分布函数/条件分布函数/条件概率密度都要写范围，顺便通过范围检查；
3. 两个随机变量的概率密度(不知道是否独立)无法得出他们俩的二维随机变量的分布情况，因此，如果题目中要求两个随机变量联合成的二维随机变量的分布，那么要么可以证明二者独立，要么其中一个随机变量可以用另外一个随机变量表示
4. 若有X的分布函数F(x)为严格单调增加函数，那么F(X)一定服从0-1上均匀分布，可以用反函数推导出，具体见例题5
5. |X-Y|可以写成max(X,Y)-min(X,Y)的形式

## 例题

1. 设随机变量X和Y同分布，已知 $P\{X=k\} = p(1-p)^k-1$ ，求 $P\{X>Y\}$
1. 设X，Y独立同分布于参数为L的指数分布，Z=max(X,Y) ，给出几个随机变量例如 $\frac{2X+Y}{3}$ ，问和Z同分布的随机变量是哪个:
    - 比较各个随机变量的取值范围
    - 可以计算Z的期望，随后和给出的随机变量期望做比较；
1. 设XY在区域 $0<x<1,x^2<y<\sqrt{}$ 上服从均匀分布，有随机变量U，在X小于Y时为1，X大于Y时为0，计算随机变量Z=U+X的分布函数
    - 注意，这种离散型和连续性函数的随机变量，如果二者不是独立的，那么使用全概率公式时要注意
1. **设XY独立同分布于标准正态分布，并且有Z，当XY>0时为X，XY=0时为0，XY<0时为-X，证明Z服从标准正态分布并证明(Y,Z)不服从标准正态分布**
5. **有XY独立同分布，并且X的分布函数F(X)为严格单调增加函数，并且E(|X-Y|)=1,那么求X和F(X)的协方差**